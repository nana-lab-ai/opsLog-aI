# OpsLog AI 開発ルール

## 1. プロジェクト概要

* OpsLog AI は、運用担当者向けのログ解析・障害対応管理Webアプリである
* サーバログ、DBログ、アプリログを扱う
* ログからエラー行を抽出する
* 障害チケットとして管理する
* 対応履歴を登録する
* 報告書ドラフトを生成する
* ディレクトリ監視によるログファイル自動取込を行う
* AIチャット風UIで、ログ・障害・対応履歴を検索できる
* 将来的に OpenAI API / Claude API 連携を追加できる設計にする

---

## 2. 技術構成

| 区分 | 内容 |
|------|------|
| 言語 | Java 17 |
| フレームワーク | Spring Boot |
| テンプレートエンジン | Thymeleaf |
| 認証 | Spring Security |
| DB アクセス | Spring Data JPA |
| DB | SQLite |
| ビルド | Maven / Maven Wrapper (`mvnw.cmd`) |
| フロントエンド | HTML / CSS / JavaScript |
| 将来対応 | OpenAI API / Claude API 連携 |

---

## 3. 現在の主要機能

* デモログイン（ID: `demo` / PW: `demo123`）
* ダッシュボード
* ログ手動登録
* ログ解析（エラー行抽出）
* ログ履歴一覧
* 障害チケット管理（作成・一覧・詳細）
* 障害詳細表示
* ステータス変更
* 対応メモ登録
* 報告書生成
* ディレクトリ監視設定（`WatchProperties` / `WatchSettingController`）
* `WatchService` によるログファイル自動取込
* AIチャット検索（`ChatSearchService`）
* チャット画面の右ペイン詳細表示（画面全体遷移なし）
* `sessionStorage` による一時的なチャット履歴保持

---

## 4. 開発方針

* このアプリは汎用CRUDではなく、運用業務改善ツールとして作る
* 新機能は、ログ調査・障害対応・初動確認・報告書作成・ナレッジ検索・運用自動化に関係するものを優先する
* 面談や職務経歴書で説明できる業務アプリとして品質を保つ
* 画面文言は日本語を基本にする
* 見た目は派手さより、業務アプリらしい見やすさを優先する

---

## 5. コーディングルール

* Controller に業務ロジックを書きすぎない
* 業務ロジックは Service に置く
* Repository はDBアクセスに集中させる
* Entity / DTO / Service / Controller の役割を明確に分ける
* Thymeleaf に複雑な業務ロジックを書かない
* 既存機能を壊さない
* 既存URLを不用意に変更しない
* 大きなリファクタリングは指示がある場合のみ行う
* 実装後は、追加ファイル・変更ファイル・操作手順・確認方法をまとめて報告する

### 主要パッケージ構成

```
com.example.opslogai
├── config/       # SecurityConfig, WatchProperties
├── controller/   # 各画面コントローラー
├── dto/          # フォーム・レスポンスDTO
├── entity/       # JPA Entity（Incident, User, ReportDraft 等）
├── repository/   # Spring Data JPA Repository
└── service/      # 業務ロジック（LogAnalysisService, AiAnalysisService 等）
```

---

## 6. セキュリティルール

* APIキー、トークン、パスワード、秘密情報をコミットしない
* 実ログ、本番ログ、個人情報、顧客情報をサンプルに使わない
* サンプルログはダミー値のみ使用する
* OpenAI API / Claude API を追加する場合も、APIキーは環境変数または外部設定ファイルに置く
* 画面には「個人情報・機密情報・本番ログは入力しないでください」という注意書きを維持する

---

## 7. ログ取扱ルール

* ログには機密情報が含まれる可能性がある
* サンプルログには実在情報を使わない
* 使用してよいサンプルログはダミーログに限定する

**許可されるダミーログ例：**

```text
ERROR DB connection timeout
WARN retry failed
ORA-12541 TNS:no listener
Connection refused
Authentication failed for dummy-user
```

---

## 8. ディレクトリ監視ルール

* ディレクトリ監視はローカル実行向け機能である
* 公開クラウド環境では正常に動作しない場合がある
* `WatchService`（`java.nio.file.WatchService`）を使う
* `ENTRY_CREATE` イベントを検知する
* `.log` / `.txt` など対象拡張子を設定できる
* ファイルコピー中の誤読を避ける（書き込み完了を待つ）
* 取込成功・失敗はアプリログに出力する
* 自動取込したログは `ImportSource.AUTO` として区別する
* 画面上でも「自動取込」と分かるバッジ表示を行う

---

## 9. AIチャット検索ルール

* 現在のAIチャット検索は、本物のLLM連携ではなく、**DB検索 + AI風回答生成**である
* 本物のAI API連携をしていない段階では、過剰に「AIが判断した」と表現しない
* チャット履歴はDBに保存しない
* `sessionStorage` で現在の画面セッション中だけ保持する
* ユーザー発言は右側、AI回答は左側に表示する
* 結果カードを表示する
* 「詳細を見る」を押した場合、画面全体遷移ではなく右ペインだけを更新する
* 右ペイン更新時も左側のチャット履歴を維持する

---

## 10. 将来的なAI API連携方針

* AI連携は Service 層に閉じ込める
* APIキーがなくてもローカルデモが起動できるようにする
* API連携失敗時はダミー生成やテンプレート生成にフォールバックできるようにする

**AI API差し替え候補：**

| クラス | 役割 |
|--------|------|
| `AiAnalysisService` | ログ解析・エラー要約 |
| `ChatSearchService.generateMessage()` | チャット回答生成 |
| `ReportService` | 報告書ドラフト生成 |

---

## 11. UIルール

* 日本語UIを基本にする
* 業務アプリらしいシンプルな画面にする
* ダッシュボード・一覧・詳細・チャットUIの整合性を保つ
* ステータス、重要度（`Severity`）、取込区分（`ImportSource`）はバッジ表示する
* ポートフォリオのスクリーンショットとして使いやすい画面にする

---

## 12. 動作確認ルール

**ローカルURL：**

```
http://localhost:18080
```

**デモログイン：**

```
ID: demo
PW: demo123
```

**起動コマンド例：**

```powershell
.\mvnw.cmd spring-boot:run
```

または

```powershell
.\run.cmd
```

---

## 13. 標準確認フロー

### ログ解析フロー

1. ログイン（`demo` / `demo123`）
2. ログ解析（新規）を開く
3. サンプルログを貼る
4. エラー行が抽出されることを確認する
5. 解析結果を確認する
6. 障害チケットを作成する
7. 障害一覧に表示されることを確認する

### ディレクトリ監視フロー

1. 監視設定を開く
2. 監視を有効にする
3. 監視ディレクトリを確認する
4. アプリ起動後に新しい `.log` ファイルを配置する
5. ログ履歴に「自動取込」として表示されることを確認する

### AIチャット検索フロー

1. AIチャット検索を開く
2. 「重要度が高い障害を教えて」と入力する
3. AI風回答と結果カードが表示されることを確認する
4. 「詳細を見る」を押す
5. 右ペインだけが切り替わることを確認する
6. 左側のチャット履歴が残ることを確認する

---

## 14. 実装完了時の報告フォーマット

Claude は実装完了時に必ず以下をまとめて報告すること。

* 追加したファイル
* 変更したファイル
* 追加したURL（エンドポイント）
* DB変更（テーブル追加・カラム追加等）
* 操作手順
* 動作確認方法
* 既知の制限
* 再起動が必要か
* 将来的なAI API差し替え箇所

---

## 15. ポートフォリオ説明文

```
Java / Spring Boot / Thymeleaf / SQLite を用いた、サーバログ解析・障害対応管理Webアプリです。
ログ手動登録、ディレクトリ監視による自動取込、障害チケット管理、対応履歴管理、報告書生成、AIチャット風検索UIを実装しています。
運用・DB・障害調査の業務改善を想定したポートフォリオアプリです。
```
